# 🛠️ دليل التثبيت والتشغيل والنشر السحابي (Installation & Deployment Guide)
### منصة معجم الأنساب والبلدان الجغرافي (MAP-DINHK)
**الإصدار**: `v2.4.0` | **البيئة المعتمدة**: Ubuntu 24.04 LTS / Docker / Coolify / Node.js 24 LTS

---

## 📋 1. متطلبات التشغيل الأساسية (System Requirements)

| المتطلب | الإصدار الموصى به | ملاحظات |
| :--- | :--- | :--- |
| **Node.js** | `v24.19.0 LTS` | محدد حتمياً في `package.json` (`>=24 <25`) |
| **مدير الحزم** | `npm` (مرفق مع Node 24) | الاعتماد على `package-lock.json` و `npm ci` |
| **نظام التشغيل الخادم** | `Ubuntu 24.04 LTS` | بيئة الإنتاج السحابية المستهدفة |
| **بيئة الحاويات** | `Docker` & `Coolify` | يدعم البناء متعدد المراحل Multi-stage build |
| **المنفذ الداخلي** | `3000` | يستمع داخلياً على `0.0.0.0:3000` |

---

## 💻 2. التشغيل والتطوير المحلي (Local Development)

### الخطوة 1: استنساخ المستودع (Clone)
```bash
git clone https://github.com/mshmsdin/MAP-DINHK.git
cd MAP-DINHK
```

### الخطوة 2: تثبيت الحزم التزامني الحتمي
```bash
npm ci
```

### الخطوة 3: تشغيل خادم التطوير المحلي
```bash
npm run dev
```
* سيفتح الموقع على الرابط: `http://localhost:5173/`

### الخطوة 4: بناء حزمة الإنتاج وتشغيل خادم الإنتاج محلياً
```bash
# بناء الأصول الإنتاجية في مجلد dist/
npm run build

# تشغيل خادم الإنتاج عالي الأداء (Node.js 24) على المنفذ 3000
npm start
```
* رابط خادم الإنتاج: `http://localhost:3000/`
* رابط فحص الصحة: `http://localhost:3000/health`

---

## 🐳 3. التشغيل عبر Docker محلياً (Docker Run)

تم إعداد ملف `Dockerfile` متعدد المراحل (`node:24.19.0-bookworm-slim`) لإنتاج حاوية خفيفة وآمنة تعمل بالمستخدم غير الجذري `USER node`.

### بناء صورة الدوكر:
```bash
docker build -t map-dinhk:latest .
```

### تشغيل الحاوية:
```bash
docker run -d \
  --name map-dinhk-app \
  -p 3000:3000 \
  --restart unless-stopped \
  map-dinhk:latest
```

### التحقق من تشغيل الحاوية وفحص الصحة:
```bash
# التحقق من سجلات الحاوية
docker logs -f map-dinhk-app

# اختبار مسار فحص الصحة
curl -f http://localhost:3000/health
```

---

## 📦 4. التشغيل عبر Docker Compose (الأسهل والأسرع)

يحتوي المستودع على ملف `docker-compose.yml` مهيأ بالكامل:

```bash
# تشغيل التطبيق في الخلفية
docker compose up -d

# إيقاف التطبيق
docker compose down

# إعادة تشغيل التطبيق بعد تحديث الكود
docker compose up -d --build
```

---

## ☁️ 5. النشر السحابي عبر Coolify خطوة بخطوة (Coolify Production Deployment)

المنصة مهيأة بنسبة 100% للعمل المباشر داخل لوحة تحكم **Coolify**:

### الخطوات داخل Coolify:
1. ادخل إلى لوحة تحكم **Coolify** في خادمك (Ubuntu 24.04 LTS).
2. انتقل إلى المشروع واضغط على **+ New Resource** ثم اختر **Application**.
3. اختر **Public Repository** (أو Private عبر حساب GitHub الخاص بك).
4. ضع رابط المستودع:
   ```text
   https://github.com/mshmsdin/MAP-DINHK.git
   ```
5. اضغط **Check repository**.
6. في إعدادات البناء (**Build Pack**):
   * اختر: **Dockerfile** (سيكتشف Coolify تلقائياً الـ Dockerfile متعدد المراحل).
7. في إعدادات المنفذ (**Ports Mapping / Exposed Ports**):
   * ضع المنفذ: **`3000`**
8. في إعدادات فحص الصحة (**Healthcheck**):
   * المسار: **`/health`**
   * الفاصل الزمني: `30s`
   * زمن الاستجابة: `5s`
9. في النطاق والدومين (**Domains**):
   * أدخل النطاق والمسار الفرعي المعتمد: **`https://din.hk/map`**
   * المنصة مهيأة بالكامل للعمل تحت المسار الفرعي `/map` (مسار أصول Vite هو `/map/`، وصفحة التعليمات الدائمة هي `/map/f1`).
   * سيتولى **Traefik** و **Cloudflare Tunnel** توجيه الترافيك وإدارة شهادات SSL/TLS تلقائياً دون الحاجة لأي تعديلات يدوية داخل الحاوية.
10. اضغط **Deploy**!

---

## 🔄 6. التحديث السلس بدون توقف (Zero-Downtime Updates)

عند قيامك بدفع أي تحديث جديد إلى فرع `main` على GitHub:
* إذا كان لديك **Webhook** مفعل في Coolify، سيتم بناء ونشر التحديث الجديد تلقائياً بمجرد إرسال إشارة الـ Push.
* أو يمكنك الدخول إلى لوحة Coolify والضغط على زر **Redeploy**.
* يدعم خادم الإنتاج إشارات `SIGTERM` و `SIGINT` لضمان الإنهاء السلس الآمن للحاوية القديمة بعد جاهزية الحاوية الجديدة تماماً.

---

## 🔍 7. فحص الصحة ومراقبة الأداء (Monitoring & Health)

* **نقطة فحص الصحة الأساسية**: `GET /health`
  * تعيد رمز `200 OK` وجسم JSON خفيف:
  ```json
  {
    "status": "ok",
    "timestamp": "2026-09-03T07:15:00.000Z",
    "uptime": 3600,
    "environment": "production"
  }
  ```
* **سجلات التشغيل (Logs)**:
  * تخرج السجلات قياسياً عبر `stdout` و `stderr` لجمعها عبر Docker أو Coolify أو Traefik.
  * كل طلب يسجل الطابع الزمني والمسار ورمز الحالة وزمن التنفيذ بالمللي ثانية دون تسجيل أي بيانات حساسة.

---

## 📜 8. الترخيص والاعتماد العلمي
* النصوص التراثية مستوعبة 100% (16,800 مادة) ومحققة وفق الطبعات المعتمدة لمبادرة النصوص المفتوحة (OpenITI).
* المنصة مفتوحة المصدر ومطابقة للمواصفات الأكاديمية والتقنية الحديثة.
